# InvestSim デザイン定義書

> 版: v1（2026-09-11）／ 正の順位: **オーナーの言葉 ＞ 本書 ＞ 各画面の実装**
> 読む人: builder（実装）・designer（設計/批評）・reviewer（レビュー）・オーナー
> 決定の理由は `DECISIONS.md` の 2026-09-11「デザイン定義書 v1」に記録。言葉の禁止事項の原本は `marketing/RULES.md`。

この文書は「InvestSim の画面は何のためにあり、どう見えるべきか」の唯一の基準です。新しい画面や部品を作るときは、まずここを読みます。書かれていないことに迷ったら、§1 の目的の一文に照らして決め、決めた内容をここへ追記します。

---

## 1. サイトの目的とゴール

### 1-1. 目的の一文

> **実データと仮想資金で、自分の投資判断を言葉にし、あとから答え合わせできるようになる練習場。**
>
> （2026-09-11 オーナー決定。legal-compliance から「できるようになる」は力が身につく約束に読め、景品表示法の優良誤認＝実際より良く見せる表示に近づくとの指摘と代案があったが、オーナー判断で原文を維持。弁護士確認事項 §11-10）

- **実データ**: 実際の株価・企業の数字を使う（COMPANY.md 原則9）。架空の数字は見せない。
- **仮想資金**: 円・ドルの単位で表示するが、実際のお金は1円も動かない（原則10）。
- **判断を言葉にする**: 売買のたびに「なぜ買う／売るのか」「どうなったら降りるのか」を書く。
- **答え合わせ**: 書いた判断と、その後の結果を突き合わせる。当たり外れより「判断の筋が通っていたか」を見る。

### 1-2. 誰のためか

| 区分 | 人物像 | このサイトでの位置づけ |
|---|---|---|
| **主** | 個別株1年目。含み損を抱えていて、買った理由を人に説明できない（PERSONA.md「リョウ」） | 全画面の設計はこの人を基準にする |
| 副 | 積立投資はしているが、個別株は未経験 | 入口（01 見る）で迷わないことを優先する |
| 対象外 | 「で、どれを買えばいい？」と答えを求める人 | 答えは出さない。画面も言葉もこの人に合わせない |

### 1-3. 利用者のゴール（画面の上で観察できる形で）

1. 買う前に、**理由**と**降りる条件**を自分の言葉で書ける。
2. 後から自分の記録を読み返し、**判断と結果のずれ**を1文で説明できる。
3. AI や著名投資家の判断と比べて、**自分の判断との違い**を言える。
4. その結果、実際の投資でも「理由を書いてから買う」習慣が残る（原則11。最終ゴール）。

### 1-4. サイトがやらないこと（デザインで守る境界）

- **答えを出さない**: 「おすすめ」「買い時」を画面にも言葉にも出さない。
- **成績で釣らない**: AI の勝率・運用成績を成果として大きく見せない（RULES.md #14）。
- **お金を動かさない**: 決済・口座連携の見た目（カード番号欄、入金ボタン等）を作らない。
- **煽らない**: 数字が回転する演出、紙吹雪、「今すぐ」などの急かしを使わない。
- **個別の相談に答えない**: 「あなたの場合は」「相談する」のような入口（チャット窓・相談ボタン）を作らない（RULES.md #12・#13）。

---

## 2. 目指す印象：「分かりやすくて、洗練された」

抽象語のままだと判断に使えないので、画面の上で確かめられる条件に分解します。

### 分かりやすい ＝

- **1画面1目的**: 1画面で頼む操作は1つ。主ボタンは1画面に1つだけ。
- **合計が先、内訳は後**: まず大きな数字を1つ出し、詳細はその下に置く（PayPay の「利用可能額」の考え方）。
- **用語には意味が付いている**: 専門用語を出したら、その場で平易な意味が読める（§6-10）。
- **色だけに頼らない**: 意味は必ず「記号・文字」と「色」の2つで伝える。

### 洗練された ＝

- **色が少ない**: 紺青（ブランド色）の塗りは主ボタンだけ。文字・線としての使い道も §5-1 の一覧に限る。それ以外は無彩色（白・グレー・墨色）。
- **揃っている**: 余白は4の倍数、角丸は3種類、文字サイズは8段階。例外を作らない。
- **静か**: 装飾・影・グラデーションに頼らず、余白と文字の太さで強弱を付ける。
- **囲わない**: 塊を枠線と角丸で囲まない。区切りは「面の色の切り替え」「1本の線」「余白」で付ける（§6-6）。
- **動きで未来感を出す**: 光る色や暗い背景ではなく、「AI が実際に何をしているか」が動きで見えることで、精密さと近未来の印象を出す（§6-19）。

### 「AI が作ったように見える」原因（2026-09-11 オーナー指摘。以後の禁止事項）

| 原因 | 禁止する形 | 代わりに |
|---|---|---|
| どの塊も同じ枠線＋同じ角丸で囲う | 角丸の枠線カードの繰り返し | 面の色の切り替え（灰の地に白い帯）と1本の区切り線 |
| 同じ大きさの箱の格子 | 数字タイル×N の横並び、同形カード×N のグリッド | 表1つ、または1行の数字。段階は番号付きの一列 |
| 説明書のひな形 | 左に目次・右に本文、節ごとに「番号＋見出し＋説明＋箱」 | 内容に合わせた構成。過程は縦の線でつなぐ（C ノート型） |
| 札と記号の散らばり | ピル型の札の多用、◯/✕ の箇条書き、絵文字の見出し | 文字で書く。札は状態にだけ |
| 全部中央揃え、字間を広げた英字風の小見出し | `text-center` `uppercase tracking-*` の常用 | 左揃え。小見出しは日本語の `small`/`--muted` |

### 避ける印象

| 避けたい印象 | そう見えてしまう原因 |
|---|---|
| トレーディング端末（玄人向け） | 暗い背景、数字の羅列、ネオン色、計器盤の多用 |
| ギャンブル・ゲーム | 緑と赤の点滅、数字のカウントアップ、勝率の強調 |
| 情報商材 | 大きな「+30万円」、断定の見出し、急かす文言 |

---

## 3. 画面の役割と主役

**主役は「見る→まねる→やる→振り返る」の4段階の流れそのもの。その心臓は 03 やる（`/trade`）**（2026-09-11 オーナー決定。COMPANY.md 原則12）。
4段階は順番そのものに意味があるので、番号（01〜04）を付けて表示し、順番は入れ替えない。

| 段階 | パス | この画面の1つの目的 | 主ボタン（1つ） | 見た目の重さ |
|---|---|---|---|---|
| 01 見る | `/watch` | AI が「何を根拠にどう判断したか」を読む | 「この判断を自分ならどうするか書く」→ /trade | 入口。落ち着いた読み物 |
| 02 まねる | `/learn` | 著名投資家の条件を過去の値動きに当てて、考え方を知る | 「この条件で自分も判断してみる」→ /trade | 補助。図が中心 |
| **03 やる** | **`/trade`** | **理由と降りる条件を書いて、仮想資金で売買する** | **「この内容で記録する」（最後の確認の段。§6-9）** | **最も重い。入力に集中できる余白** |
| 04 振り返る | `/review` | 書いた判断とその後の結果を突き合わせる | 「次の判断に活かすメモを書く」（メモ機能と保存先は未実装。別スライス） | 03 と対になる。記録が主役 |
| 補助 | `/simulate` | 過去データでのバックテスト（過去の値動きに条件を当てた検証） | ─ | ナビに出さない |

**画面をまたぐときの引き継ぎ（法務条件）**: 01 見る・02 まねる から 03 やる へ移るときは**銘柄だけ**を引き継ぐ。AI や著名投資家の「買い／売り」を最初から選んだ状態にしない（判断をなぞらせると投資助言に近づくため。2026-09-11 legal-compliance）。

### トップページの構成（上から順に）

1. 目的の一文（見出し）＋「仮想資金・実データ」の札
2. 4段階の流れの図。**03 やる を一回り大きく**描く
3. 主ボタン1つ（状態で出し分ける）
   - 記録がまだ無い人 →「01 見る から始める」
   - 記録がある人 →「03 やる：続きを書く」
4. 何が残るのか（判断の記録の実物の見本。**画面の実物のみ、成績の数字は出さない**）
5. 免責表示（§6-11）

---

## 4. デザインの原則（判断に迷ったときの基準）

PayPay の公開情報から転用したもの（出典は §12）と、InvestSim 固有のものを並べます。

| # | 原則 | 具体的には | 由来 |
|---|---|---|---|
| P1 | **1画面1目的、入力は1問ずつ** | 理由の入力は「買う理由」「降りる条件」を1問ずつ見せる。長いフォームを1枚に並べない | PayPay（口座登録を1項目1画面に）[7] |
| P2 | **煽らず、直接的に書く** | 宣伝調より、何が起きるかをそのまま書く方が人は動く。ボタンは結果を表す動詞で | PayPay の A/B テスト結果 [10] |
| P3 | **合計を先に1つ、内訳は後** | 総資産（仮想）を1つ大きく出し、銘柄の内訳は下の一覧に | PayPay「利用可能額」[15] |
| P4 | **大事な状態は、色と大きな文字の両方で** | 「仮想資金」「確認画面の内容」は色だけでなく文字で大きく示す | PayPay（支払い方法を色＋大きな表示で）[8] |
| P5 | **ブランド色は「主な操作」と「いまいる場所」に絞る** | 紺青の塗りは主ボタンだけ。使い道は §5-1 の一覧に限り、見出し・飾りには使わない | PayPay（下部ナビは現在地だけ赤）[6] |
| P6 | **難しい言葉は言い換え、注意書きは決まった位置に** | 用語には意味を添える。免責は共通部品で、毎回同じ場所に | PayPay（UXライター・ポイント運用の注記）[11][19] |
| P7 | **ルールは「良い例・悪い例・理由」の3点で書く** | 本書の部品の節は「やること／やらないこと」をセットで書く | PayPay スタイルガイドの構成 [2] |
| P8 | **売買の方向に色を付けない** | 買い/売りは ▲/▼ と文字で。緑/赤は損益だけ | InvestSim（DECISIONS 2026-09-10） |
| P9 | **数字は実物だけ。取れなければ「取れなかった」と出す** | 取得できない値を仮の数字で埋めない | InvestSim 原則9 |
| P10 | **当たり外れより判断の筋を目立たせる** | 振り返り画面では、損益の数字より「書いた理由」を先に・大きく | InvestSim 原則11 |

---

## 5. デザイントークン（名前の付いた値）

トークンとは、色や余白に「役割の名前」を付けた値のことです。画面では `#1A4787` のような生の値を直接書かず、必ず `--brand` のような名前で使います。こうすると、あとで色を変えたくなったときに1か所直すだけで全画面が変わります。
**実装の置き場所**（Tailwind v4。Tailwind はクラス名で見た目を書く道具）:
- **値**は `app/globals.css` の `:root` に1か所だけ置く（例 `--brand: #1A4787;`）。
- **クラスにする登録**は同じファイルの `@theme inline` で行う（例 `--color-brand: var(--brand);` → `bg-brand` `hover:bg-brand-strong` `text-brand` が使える）。`@layer utilities` に自作クラスを書く今のやり方では、Tailwind が `hover:` などの状態付きクラスを作らないため使わない。
- 角丸と文字サイズは、Tailwind の既定の名前（`rounded-md` `text-sm` など）を上書きしない名前で登録する（例 `--radius-field` `--radius-card` → `rounded-field` `rounded-card`、`--text-display` `--text-caption`）。既定を上書きすると、今それを使っている画面が黙って変わるため。

### 5-1. 色

ロゴの紺青を軸にし、グレーもわずかに青みに寄せて、ロゴと画面を同じ家族に見せます。
コントラスト比（文字と背景の濃さの差。WCAG 2.1 の式で実測、2026-09-11 MC 計測）は、文字 4.5:1 以上、部品の輪郭 3:1 以上、区切り線 1.3:1 以上を要件とします。

**面（背景）**

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#F6F8FB` | ページの地。わずかに青みのオフホワイト |
| `--card` | `#FFFFFF` | 手前の紙。カード・入力画面の面 |
| `--surface` | `#EDF1F6` | 一段くぼんだ面。カードの中の行・入れ子のブロック |
| `--brand-tint` | `#E8EEF7` | 「いまいる場所」「選択中」の薄い下地 |

**文字**（数字は各面の上での実測値。card / bg / surface / brand-tint の順）

| トークン | 値 | 用途 | 実測 |
|---|---|---|---|
| `--ink` | `#0D1725` | 本文・見出し・金額（ロゴの文字と同じ色） | 18.00 / 16.92 / 15.87 / 15.44 |
| `--ink-2` | `#3A4658` | 説明文 | 9.56 / 8.98 / 8.43 / 8.20 |
| `--muted` | `#5B677A` | 補足・ラベル・注記 | 5.73 / 5.38 / 5.05 / 4.91 |

**ブランド（紺青）**

| トークン | 値 | 用途 | 実測 |
|---|---|---|---|
| `--brand` | `#1A4787` | 主ボタンの塗り・現在地の文字・リンク | 9.15 / 8.60 / 8.07 / 7.84 |
| `--brand-strong` | `#143A6E` | 主ボタンのホバー・押下 | 11.29（白の上） |
| `--on-brand` | `#FFFFFF` | 紺青の塗りの上の文字 | 白 on brand 9.15 |
| `--focus` | `#2566B2` | キーボード操作時の枠（ロゴの「i の点」の色） | 5.80 / 5.45 / 5.11 / 4.97 |

**紺青の使い道（この一覧が唯一の定義。他の節はここを参照する）**
- 塗り: **主ボタンだけ**
- 文字・線: 現在地（ナビ・タブ）、選択中の枠、リンク、チャートの「あなた」の線
- 薄い下地 `--brand-tint`: 現在地・選択中の下地だけ（札やカードの面には使わない）

**意味の色（ブランド色とは別物。役割を混ぜない）**

| トークン | 値 | 用途 | 実測 |
|---|---|---|---|
| `--success` / `--success-tint` | `#177A4F` / `#E9F5EF` | **利益（プラスの損益）だけ** | 5.34 / 5.02 / 4.71 ／ tint 上 4.77 |
| `--danger` / `--danger-tint` | `#C03535` / `#FBECEC` | **損失（マイナスの損益）・入力の誤り・元に戻せない操作だけ** | 5.52 / 5.19 / 4.87 ／ tint 上 4.82 |
| `--warning-ink` / `--warning-tint` | `#8A5300` / `#FDF3E1` | 注意（データが古い・取得できなかった等） | 6.33 / 5.95 / 5.58 ／ tint 上 5.75 |

緑/赤は「状態」（損益・誤り）にだけ使い、**売買の方向やチャートの系列には使わない**（DECISIONS 2026-09-10 の不変条件と同じ意味）。

**線**

| トークン | 値 | 用途 | 実測 |
|---|---|---|---|
| `--border` | `#C3CCD8` | カードの輪郭・行の区切り（区切り線 1.3 以上） | 1.62 / 1.52 / 1.43 / 1.39 |
| `--border-input` | `#7C889B` | 入力欄・副ボタンの輪郭（部品 3:1 以上） | 3.59 / 3.37 / 3.16 / 3.08 |

**チャートの系列色**（線・点など図形にだけ使う。文字には使わない。系列は色に加えて線の形でも区別する）

| 系列 | 値 | 線の形 |
|---|---|---|
| あなた | `#3468C0`（紺青を線用に明るくした青） | 実線・太め（2px） |
| AI | `#8E5BC4`（紫） | 破線（2px） |
| 著名投資家 | `#C26A2A`（橙） | 点線（2px） |
| 基準（元本・指数） | `#7C889B`（＝`--border-input`。白の上 3.59） | 細い実線（1px） |

- 3色は `dataviz` スキルの `validate_palette.js`（色の見え方が違う人でも見分けられるかの検査）で**5項目すべて合格**（2026-09-11 MC 実行。見え方の差が最も小さい組でも ΔE 14.8、目標 8 以上）。
- 当初案（紺青 `#1A4787`・紫 `#8A5CB0`・青緑 `#0B8C8E`）は不合格だった。紺青は線としては暗すぎ、青緑は鮮やかさが足りず灰色に見え、紫と青緑は赤緑の見分けが弱い人に区別しにくい（ΔE 7.2）。
- `--brand` `#1A4787` は文字と塗りの色のまま。チャートの線だけ `#3468C0` を使う。
- 系列が1本だけの図（例: 株価と、その平均線）は、主役の線だけ `#3468C0` にし、平均線などの補助の線は `--muted` `#5B677A` の破線・点線＋線の端に名前を書く（色の見分けに頼らない）。

#### 色のルール

| やること | やらないこと |
|---|---|
| 紺青は §5-1 の一覧の使い道だけ（塗りは主ボタンだけ） | 見出しや飾りを紺青で塗る。紺青の面を広く敷く |
| 損益は「符号付きの数字＋色」で伝える（例: `+$123.40`。記号の扱いは §11-1） | 色だけで損益を伝える。買い/売りを緑/赤で塗る |
| 買い/売りは ▲/▼ と文字、色は `--ink` | 「買い＝緑・売り＝赤」の金融の慣習に合わせる |
| 取得できない値は `--warning-ink` の注記で「取得できませんでした」 | 取得できない値を 0 や前回値で黙って埋める |
| 著名投資家の線（青緑）と損益の塗り（緑）を同じ図に置かない | 青緑と緑を並べて見分けさせる |

### 5-2. 文字

**書体**（今の実装を引き継ぐ）

| 役割 | 書体 | 備考 |
|---|---|---|
| 和文 | Noto Sans JP（セルフホスト） | 本文・見出しすべて |
| 欧文・数字 | Geist | 数字は必ず `font-variant-numeric: tabular-nums`（桁の幅を揃え、縦に並べたとき桁が揃う） |
| 等幅 | 使わない | ティッカー（銘柄コード。例 AAPL, 7203.T）も通常書体の太字で表す。書体の種類を減らす方が洗練して見える |

**文字サイズの段階（8段階。これ以外を使わない）**

| 名前 | px | 行間 | 太さ | 用途 |
|---|---|---|---|---|
| `display` | 40 | 1.2 | 600 | 総資産（仮想）の大きな数字。**1画面に1つまで** |
| `amount` | 32 | 1.2 | 600 | 確認画面の金額、カード内の主な数字 |
| `h1` | 24 | 1.4 | 600 | ページの見出し（1ページ1つ） |
| `h2` | 20 | 1.4 | 600 | 節の見出し |
| `h3` | 18 | 1.5 | 600 | カードの見出し・小見出し |
| `body` | 16 | 1.8 | 400 | 本文・入力欄（スマホで入力欄が勝手に拡大されない最小値） |
| `small` | 14 | 1.7 | 400 | 補足・表・ラベル |
| `caption` | 12 | 1.6 | 400 | 注記・免責・チャートの軸。**これより小さい文字は作らない** |

- 太さは **400（通常）と 600（強調）の2つだけ**。700 以上は使わない。
- 見出しは `text-wrap: balance`（行の長さを揃えて、1文字だけ次の行に落ちるのを防ぐ）。
- 読み物の1行は全角 35〜40 字程度（最大幅 640px）に収める。

**数字の書き方**

| 項目 | 規則 | 例 |
|---|---|---|
| 桁区切り | 3桁ごとにカンマ | `1,234,567` |
| 円 | 整数。単位は数字の後ろに小さく（0.75em） | `1,234,567`<small>円</small> |
| ドル | 小数2桁。記号は数字の前 | `$178.20` |
| 符号 | プラスは `+`、マイナスは `−`（U+2212。ハイフンより幅が揃う） | `+3.2%` / `−1.8%` |
| ゼロ | `±0`、色は `--muted` | `±0円` |
| 割合 | 小数1桁 | `12.5%` |
| 仮想資金 | 総資産の数字の近くに必ず「仮想資金」の札（§6-3） | ─ |

### 5-3. 余白（4の倍数だけ）

| トークン | px | 主な用途 |
|---|---|---|
| `space-1` | 4 | アイコンと文字の間 |
| `space-2` | 8 | 札の内側、密な並び |
| `space-3` | 12 | ラベルと値の間 |
| `space-4` | 16 | カードの内側（スマホ）、画面の左右（スマホ） |
| `space-6` | 24 | カードの内側（PC）、画面の左右（PC）、カード同士の間 |
| `space-8` | 32 | 小さな節の間 |
| `space-12` | 48 | 大きな節の間 |
| `space-16` | 64 | ページの上下 |

- 並んだ要素の間隔は、個々の要素の外側余白（margin）ではなく、並べる側の `gap` で取る（余白が二重になったり消えたりしない）。
- 最大幅: 読み物 640px、一覧・操作画面 1120px。
- 余白の値は Tailwind の既定（`p-1`＝4px 〜 `p-16`＝64px）と同じなので、新しいトークンは作らず既定のクラスを使う。
- 線の太さ（1〜2px）とキーボード操作の枠の隙間（2px）は「4の倍数」の対象外。

### 5-4. 角丸（3種類だけ）

| トークン | px | 用途 |
|---|---|---|
| `--radius-field`（クラス `rounded-field`） | 6 | 入力欄・小さな札・チェックボックス |
| `--radius-card`（クラス `rounded-card`） | 12 | 白い帯の端・ボタン・シート・トースト |
| `rounded-full` | 999 | 段階ナビの現在地、方向・状態の札（ピル型） |

登録名を `field`/`card` にしたのは、Tailwind の既定 `--radius-sm`（4px）/`--radius-md`（6px）と名前が衝突し、既存画面が黙って変わるため（2026-09-11 architect 実測）。本書で「radius-sm / radius-md」と書いている箇所は、それぞれ `rounded-field` / `rounded-card` と読む。

角丸を付けるのは、ボタン・入力欄・札・浮いているもの（シート・トースト）と、白い帯の上下の端（`radius-md`）だけ。**読むだけの塊に角丸の枠を付けない**。

### 5-5. 影（浮いているものだけ）

| トークン | 値 | 用途 |
|---|---|---|
| なし | ─ | カード・一覧。**影ではなく、面の色と枠線で区切る** |
| `shadow-float` | `0 8px 24px rgb(13 23 37 / 0.12)` | 下から出るシート、モーダル、トースト、画面下に貼り付く操作バー |

### 5-6. 動き

| 項目 | 規則 |
|---|---|
| 速さ | ホバー・押下 150ms、シート・トーストの出入り 250ms、`ease-out` |
| 使う場面 | 状態が変わったことを伝えるとき（開く・閉じる・記録された）と、**AI の処理の過程を見せるとき**（§6-19。件数の数え上げ・線の描画はここでだけ許す） |
| 使わない場面 | 損益の数字のカウントアップ、損益の点滅、紙吹雪、読み込み中以外のループ、光る縁取り・発光・走査線の飾り |
| 配慮 | `prefers-reduced-motion`（端末で「動きを減らす」設定）のときは動きをなくす |

---

## 6. 部品（コンポーネント）

各部品は「目的／形／やること／やらないこと」で書きます。実装は `components/ui/` に共通部品として1つずつ作り、画面ごとに手書きしない。

### 6-1. ボタン

| 種類 | 形 | 使う場面 |
|---|---|---|
| **主** | 高さ 48px、`--brand` の塗り、白文字 16px/600、`radius-md` | その画面の1つの目的を実行する。**1画面に1つ** |
| 副 | 高さ 48px（小は 44px）、`--card` の面＋`--border-input` の輪郭、`--ink` 文字 | 主以外の操作（戻る、別の選択肢） |
| 文字 | 枠なし、`--brand` 文字、ホバーで下線 | 補助的な移動（「詳しく見る」） |
| 取り消し | 副の形で文字が `--danger`。確定ボタンだけ `--danger` の塗り＋白文字 | 記録の削除など、元に戻せない操作だけ |

- ボタンの文言は「押すと何が起きるか」を動詞で書く: 「この内容で記録する」「内容を確認する」（◯）／「OK」「送信」「次へ」だけ（✗）。
- 押せない状態は `--surface` の面＋`--muted` の文字にし、**押せない理由をすぐ近くに書く**。
- 押した後は必ず結果を示す（トースト「記録しました」、または次の画面へ）。

**売買の選択（/trade）**

- 「▲ 買う」「▼ 売る」は**同じ重さの副ボタン**として並べる。どちらかを主ボタンにしない（買いへ誘導しない）。
- 選んだ方は `--brand-tint` の面＋`--brand` の輪郭 2px＋文字「選択中」。
- 緑/赤で塗らない（P8）。記録を確定するのは、最後の確認の段の主ボタン「この内容で記録する」だけ（§6-9）。
- 他の画面から来たときは銘柄だけを引き継ぎ、買う/売るは選ばれていない状態で始める（§3 の法務条件）。

### 6-2. お金の数字（金額表示）

```
総資産  [仮想資金]             ← small / --muted のラベル＋仮想資金の札（6-3）
$101,234.56                   ← display 40px / --ink / tabular-nums
+$1,234.56（+1.2%）開始時から   ← small、損益の規則（6-4）に従う
9/11 15:00 時点               ← caption / --muted
```

- 仮想資金は**ドル建て**（開始時 $100,000。`lib/portfolio.ts:48`）。銘柄ごとの株価は、その銘柄の通貨で表示する。

- **ラベル → 大きな数字 → 補足** の3段で必ず組む。
- 1画面に `display` サイズの数字は1つだけ。内訳は一覧（6-6）で小さく。
- 数字の横幅がぶれないよう、必ず `tabular-nums`。
- 数字の近くに、いつ時点の値かを書く（`caption`：「9/11 15:00 時点」）。**総資産だけでなく、株価・チャートすべて**に付け、「遅れている場合があります」を添える。

### 6-3. 仮想資金の札

- 形: `--card` の面＋1px の `--border-input` の枠＋`--ink` の文字、角丸 6px、`small`/600、文字「仮想資金」。**ピル型（角丸 999）にしない**（現在地のピルと同じ形だと、押せるボタンに見えるため）。
- 位置: 総資産・残高の数字の**真上か真横**。スクロールで数字だけが見える状態を作らない。
- 目的: 本物のお金と誤解させない（P4）。

### 6-4. 損益の表示

| 状態 | 表示 | 色 |
|---|---|---|
| 利益 | `+$123.40（+3.2%）` | `--success` |
| 損失 | `−$81.20（−2.1%）` | `--danger` |
| ゼロ | `±$0.00（0.0%）` | `--muted` |

- 符号は必ず付ける（色が見分けにくい人にも伝わる）。記号の扱いは §11 の未決1を参照。
- **振り返り（/review）では、損益の数字より「書いた理由」を先に・大きく置く**（P10）。

### 6-5. 方向・状態の札（バッジ）

| 札 | 表示 | 形 |
|---|---|---|
| 買い | `▲ 買い` | `--surface` の面＋`--ink` の文字、`radius-full` |
| 売り | `▼ 売り` | 同上 |
| 保有継続 | `＝ 保有継続` | 同上 |
| 様子見 | `◇ 様子見` | 同上 |
| 注意 | `! データが古い` など | `--warning-tint` の面＋`--warning-ink` の文字 |

- 方向の札に色は付けない。区別は記号と文字で（P8）。
- 1つの行に札は2つまで。

### 6-6. カード・一覧の行

2026-09-11 オーナー決定: **操作する画面は A アプリ型、記録を読む画面は C ノート型**。比較ページ https://claude.ai/code/artifact/b7d141ff-a209-4fd8-8c59-dac409de9fef

**A アプリ型（/ ・/watch の判断一覧・/trade・/learn・一覧や入力のある画面）**
- ページの地は `--surface`（灰）。内容は **端から端までの白い帯**（`--card`）に載せる。帯には枠線を付けない。帯の上下の端だけ `radius-md`。
- 見出しは帯の外（灰の上）に `small`/`--muted` で置く。帯の中に見出しを入れない。
- 帯の中の行は高さ 56px 以上。左に名前（`body`/600）とティッカー（`small`/`--muted`）、右に数字（右揃え・`tabular-nums`）。行の区切りは **文字の左端から始まる** 1px の `--border`。行全体を押せる範囲にする。
- 帯と帯の間は 8px。節の間は 24px。主ボタンは画面の下に固定（スマホは下部ナビの上）。
- PC では中央に幅 640〜760px に絞る（間延び防止）。

**C ノート型（/review・判断の記録一覧・§6-15・§6-19）**
- 白い地。左に日付（または段の番号）、その右に **縦の線** を1本引き、記録を線に沿って並べる。各記録の位置に小さな丸印。
- 枠線・面の色の差は使わない。記録と記録の間は 24〜28px の余白で分ける。
- 入力欄は罫線のあるノートの形（下線の繰り返し）。

**どちらでも共通**
- 角丸の枠線で塊を囲わない。**囲いを使ってよいのは、押せる塊（ボタン・選択肢）、浮いているもの（シート・トースト・検索候補）、入力欄だけ**。
- 帯の中に帯を入れない（入れ子禁止）。今の実装は約24か所で破っている（2026-09-11 designer 実測: 囲い約245か所・38ファイル、/watch 約35個・/learn 約40個・3段の入れ子あり）。
- 数字タイルの格子は作らない。複数の数字は表1つか、1行の数字にする。

### 6-7. ナビゲーション（4段階）

- **PC**: ヘッダーにロゴ＋4段階。**スマホ**: 画面下に4段階（ロゴはマークのみ）。
- 現在地: `--brand-tint` のピル＋`--brand` の文字/600。それ以外: `--muted` の文字。
- 番号（01〜04）は常に表示する（順番に意味があるため）。
- 「● 運用中」のような状態表示は、**実際にその状態のときだけ**出す。常時点けない。
- 画面上部に貼り付く（sticky）帯は1本だけ。2本重ねない。

### 6-8. タブ

- 現在のタブ: `--ink` の文字/600＋下に 2px の `--brand` の線。それ以外: `--muted`。
- タブは4つまで。それ以上は一覧か別画面に分ける。

### 6-9. 理由の入力欄（/trade の中心部品）

- **問いの中身は `lib/trade/reason.ts` が正**（DECISIONS「理由を問いに分解する」）。本書は見た目と順番だけを決める。
  - 買い: 見立て「なぜ買うのか」（必須・10字以上）／注目「これから何を見るか」（任意）／降りる条件「何が起きたら『間違いだった』と認めるか」（必須・6字以上）
  - 売り: 売る理由「なぜ売るのか」（必須・10字以上）／変化「買ったときの見立てから何が変わったか」（任意）
- **1画面1目的は「段」の単位で守る**（P1）。1つの段の中に、同じ目的の問いが複数あってよい。進み具合は「2 / 3」のように文字で示す。

| 段 | この段の目的 | 主ボタン |
|---|---|---|
| 1 / 3 | 銘柄・▲買う/▼売る・株数を決める | 「理由を書く」 |
| 2 / 3 | 理由を書く（問いは上記） | 「内容を確認する」 |
| 3 / 3 | 確認: 銘柄・方向・株数・金額（`amount` サイズ）・書いた理由を大きく再掲（PayPay の確認画面の考え方 [8]） | **「この内容で記録する」**（記録を確定するボタンはこれ1つだけ） |

- 各段に副ボタン「戻る」。戻っても書きかけの内容は消さない。
- 形: 問いは `h3`、その下に `small`/`--muted` の書き方のヒント、入力欄は `--card` の面＋`--border-input` の輪郭、角丸 6px、文字 16px。任意の問いには「任意」と文字で書く。
- 入力の誤りは入力欄の直下に `--danger` で「何が足りないか」と「どう直すか」を書く（例:「降りる条件は6文字以上で書いてください。『決算で売上が前年を下回ったら』など」）。
- 今の `/trade` は1〜2段が1画面にまとまり、確認の段が無い。段に分けるのは `/trade` を作り直すスライスで、DECISIONS に追記してから行う（§11-12）。

### 6-10. 用語の説明

- 文中の専門用語は、太字（600）＋点線の下線で示す。
- PC: ホバーに加え、キーボードで選んだとき・タップしたときにも短い意味（30字以内）を表示。スマホ: 文の下に折りたたみ「この文に出てきた用語」。
- 説明は売買を促さない（「割安だから買い」等を書かない）。原本は `lib/ai-trader/glossary.ts`。

### 6-11. 免責表示（共通部品）

- **ページごとに手書きしない**。`components/ui/Disclaimer` の1つだけを使う（RULES.md §6 P0）。
- 位置: (a) 判断・数字を出すブロックの直下、(b) 全ページ共通のフッター。**毎回同じ位置**に置く（P6）。
- 形: `small`（14px）・`--ink-2` の文字に固定。`caption`・`--muted` に下げない（`app/page.tsx:190` で読みやすく上げた修正を戻さない）。折りたたみの中に隠さない。
- 文言は「目的の表明」にする。法的評価を自称しない（「投資助言・代理業には該当しません」とは書かない）。
- **文言の正は `marketing/RULES.md` §2(a) の短い版**。部品の中で文言を作り直さない。
- 著名投資家の名前が出る画面では「ご本人とは無関係です（公開されている考え方を当てはめたもの）」の1文を加える（RULES.md「著名投資家名を出す投稿に必須の1文」）。
- フッターには、運営者情報・問い合わせ・プライバシーポリシーへのリンク枠を置く（中身が未整備でも枠は用意する）。
- 「無料」と書く画面では「現在は無料です。将来、有料の機能を作る可能性があります」を添える（RULES.md #22）。

### 6-12. 空・読み込み中・エラー

| 状態 | 書くこと | やらないこと |
|---|---|---|
| 空（記録が無い） | 何が無いか＋次の一手のボタン1つ（例:「まだ判断の記録がありません」→「03 やる で最初の記録を書く」） | 見本の架空データで埋める |
| 読み込み中 | 完成時と同じ形の薄い枠（スケルトン）。3秒を超えたら「株価を取得しています」と書く | 画面全体をぐるぐるで覆う |
| エラー | 何が起きたか／データはどうなったか／どうすればいいか（例:「株価を取得できませんでした。記録は保存されています。時間をおいて再読み込みしてください」） | 謝るだけ、原因を書かない、仮の数字を出す |

### 6-13. トースト・確認シート

- **トースト**（数秒で消える小さな通知）: 画面下、4秒、1行。成功の報告だけに使う（「記録しました」）。操作が必要なエラーには使わない。スマホでは下部ナビ（高さ 56px）の上に出す。画面下に貼り付く操作バーも同じ。
- **確認シート／モーダル**: スマホは画面下から出るシート、PC は中央。背景は `rgb(13 23 37 / 0.32)` で暗くする。`shadow-float`、`radius-md`。閉じるボタンと Esc キーで閉じられる。

### 6-14. チャート

- 系列色と線の形は §5-1 の表のとおり。**色の定義は `components/chartTheme.ts` の1か所だけ**にし、各チャートはそこから読む。
- 軸の文字は `caption`/`--muted`、補助線は `--border` を薄く（50%）。
- 最新の点を強調し、その値を線の端にラベルで出す。
- 売買の印は ▲/▼＋「買 178.20」「売 191.40」、色は `--ink`（P8）。
- 凡例の色と線の色を必ず一致させる（同じトークンから取る）。
- 複数の系列（あなた・AI・著名投資家）を並べる図は、開始時点を 0% とした増減率で描き、縦軸は1本にする。

### 6-15. 判断記録カード（/review の中心部品）

- 並び順は **①書いた理由 → ②その後の値動き → ③損益 → ④ずれのメモ** の順。損益を先頭にしない（P10）。
- ① 見出し付きで表示（見立て・注目・降りる条件／売る理由・変化）。見出しの無い古い記録は、そのまま1つの文として表示する。
- ② 記録した時点の株価 → 今の株価を、それぞれ時点付きで。
- ③ `small` サイズの損益（§6-4）。
- ④ 未実装（別スライス）。
- 過去の売買の記録で「降りる条件」が空のときは「決めていなかった」と表示する。責める言い方にしない（DECISIONS「過去の記録では降りる条件を必須にしない」）。

### 6-16. データの状態（鮮度・AIの判断中・市場の時間外）

- すべての株価・チャートに取得時刻（§6-2）。
- 「古い」の札（`! データが古い`、§6-5）を出すまでの時間は data-engineer が決める（§11-13）。
- AI が判断を書いている最中: 「AI が判断を書いています」＋完成時と同じ形の薄い枠。
- 市場の時間外: 無彩色の札「市場は閉まっています」＋次の取引開始の時刻。異常ではないので注意色にしない。

### 6-17. ログインしていないとき

- 主ボタンは1画面に1つのまま。ヘッダーの「ログイン」は文字ボタンにする（塗りボタンにしない）。
- ログインを求めるのは、記録しようとした時点（3 / 3 の段）。「記録を残すにはログインが必要です」＋主ボタン「ログインして記録する」。書きかけの内容はログイン後も残す。

### 6-18. 入力部品と表

- **銘柄検索**: 入力欄＋候補一覧（名前＋ティッカー）。キーボードの ↑↓ で選べる。候補が無いときは「見つかりません。ティッカー（例 AAPL、7203.T）でも探せます」。
- **株数**: 数字の入力欄（16px）＋増減ボタン（44px 以上）。買える上限の株数を近くに書く。
- **選択チップ**（期間の切り替えなど）: 角丸 6px。選択中は `--brand-tint` の下地＋`--brand` の枠。
- **表**: 見出し行は `small`/`--muted` を `--surface` の上に、数字は右揃え、行の区切りは `--border`。スマホでは表の枠の中だけで横にスクロールさせる。

### 6-19. 分析の過程の再生（/watch の中心部品・未来感の出どころ）

2026-09-11 オーナー決定。動く見本（本物の第72回の記録）: https://claude.ai/code/artifact/a554ecc6-8b8c-42d6-bf7a-ee4639af40a6 。オーナー評価「動いている感じは出ている・速さも丁度いい・正直にいきたい」。

**目的**: 「AI がいま何を集め、何を計算し、どう判断したか」が動きで見えることで、判断の根拠を学べる（原則11）と同時に、精密さ・近未来の印象を出す。JARVIS（映画の AI）の魅力のうち「分析の過程が動きで見える」部分だけを取り、暗い背景・光る線・答えを出す相棒の要素は取らない。

**段の構成（`engine.ts` の実際の処理順と1対1。順番を入れ替えない）**

| 段 | 見せるもの | 動き |
|---|---|---|
| 1 候補を選ぶ | 40銘柄のティッカーの格子（これは「集合」を表す唯一の格子として許す）→ 保有中 → 分析対象 | 走査して1つずつ光らせ、件数を数え上げる |
| 2 材料を集める | 3か月の終値の線、会社の数字の札、ニュース | 線を左から描き、札を順に出す |
| 3 指標を計算 | 20日・50日平均線、現在値との並び、RSI・MACD | 平均線を1本ずつ足し、判定の文を出す |
| 4 知識を読む | 選ばれた知識の件数と題名 | 件数だけ |
| 5 AI に聞く | 銘柄の札が1つにまとまって送られる → 「書いています」 | 札の移動と点の明滅 |
| 6 判断 | 銘柄ごとの判断の行 → 注目銘柄の理由 | 行を上から順に出す |

**守る条件**
1. **動きは本物の処理と1対1**。結果が先に出ているのに「計算しているふり」をしない。途中に出す件数・数字はすべて実際の値。
2. **出どころを印で示す**。各段に「記録から」「同じ取得元から計算し直した」「記録なし」のどれかを必ず付ける。記録にないものは、それらしい値で埋めず「記録なし」と書く（原則9）。
3. **2回目からは短く**。ページを開いたときは完成した静止状態を見せ、「過程を再生する」を押したときだけ動かす。ふつうの速さで約15秒、「3倍」と「結果まで飛ばす」を常に置く。`prefers-reduced-motion` のときは動かさない。
4. **結果は内訳で見せる**。判断の一覧は「保有継続・様子見・買い・売り」を同じ重さで並べ、勝率や「AI が当てた」の見せ方にしない（RULES.md #14・#17）。判断の記録が無い銘柄も行として出す。
5. **点数は付けない**。分析は事実を並べるところまで。
6. **見た目は A＋C**。段は C ノート型（左に段の番号、縦の線、丸印）、各段の中身は白い帯。走査中の強調は `--brand-tint`、確定は `--brand`。光る縁取り・発光は使わない。

**実装の前提（記録に足すもの・NEXT-STEPS 参照）**: tick の開始時刻と各段の所要時間／候補40銘柄の変化率と順位／MA・RSI・MACD・BB の数値（`techDetail` を捨てずに保存）／ニュース5件／モデル名・トークン数・stop_reason。足すまでは、その段を「記録なし」「再計算」と表示して出す。

---

## 7. 言葉のルール（画面の文言）

画面の文言もデザインの一部です。原本は `marketing/RULES.md` の §1（NGワード28項目）。画面で特に効くものを抜き出します。

| やること | やらないこと |
|---|---|
| 何が起きるかを直接書く（「実際のお金は1円も動きません」） | 「儲かる」「勝てる」「必ず」「上手くなります」（#1〜3・#7） |
| 判断の材料として示す（「AIの判断を読む画面です」） | 「おすすめ」「買い時」「注目銘柄」「AIが選んだ」（#8・#15） |
| 分からないことは「分かりません」と書く | 「〜と思われます」で予測を濁す（#27） |
| 結果の数字は画面の実物だけ、時点を添える | 「AIの勝率◯%」を成果として掲げる（#14）、過去検証の年利を約束のように出す（#19） |
| 主語を自分にする・事実だけを書く | 「初心者でも簡単に」「たった◯分で」「知らないと損する」（#23〜25） |
| 専門用語には、その場で平易な意味を添える | 用語を出しっぱなしにする |
| ボタンは結果を表す動詞（「理由を書いて記録する」） | 「OK」「送信」「次へ」だけ |
| エラーは原因と直し方を書く | 「エラーが発生しました」「申し訳ありません」だけ |

- **「リスクゼロ」は画面で使わない**。「実際のお金は1円も動きません」に統一する（#4。2026-09-11 legal-compliance）。
- 免責文があっても断定を書いてよいことにはならない（#28）。

---

## 8. 使いやすさの数値基準

| 項目 | 基準 |
|---|---|
| 文字のコントラスト | 4.5:1 以上（§5-1 の実測表を満たすトークンだけを使う） |
| 部品の輪郭・図形 | 3:1 以上 |
| 押せる範囲 | 44×44px 以上（主ボタンは高さ 48px） |
| キーボード操作の枠 | `--focus` の 2px 線＋2px の隙間。**標準の枠を消すなら必ずこれに置き換える** |
| 色だけに頼らない | 損益・方向・状態はすべて記号か文字を併記 |
| 最小の文字 | 12px。本文と入力欄は 16px |
| スマホ幅 | 390px で横にはみ出さない。表・図は自分の枠の中だけで横スクロール |
| 動き | `prefers-reduced-motion` を尊重 |

---

## 9. テーマ（明るい/暗い）

- **ライト（明るい配色）固定を続ける**（2026-09-07 決定。入門者向けなのに暗い背景はトレーディング端末に見えるため。`app/layout.tsx`）。
- ダークモード（暗い配色）は作らない。作る場合は本書に章を追加してから。

---

## 10. 今の実装との差分（移行チェックリスト）

2026-09-11 の designer 棚卸しによる。上から順に直す。**実装は builder が、1項目ずつ小さく切って行う**。

### P0：壊れている・読めない（見た目の統一より先に直す）

- [ ] 緑/赤の塗りボタンに濃い文字（約 3.3:1 と 3.2:1）、ホバーで反転: `app/trade/page.tsx:374`、`components/TradeButton.tsx:20`
- [ ] 選択中の買う/売るも約 3.3:1: `app/trade/page.tsx:331`
- [ ] トップの「保有継続」の札が約 1.8:1: `app/page.tsx:41`
- [ ] 定義の無いクラス名で色が付いていない: `text-bull`・`text-bear`（`InvestorPanel.tsx:18`、`RealtimeQuote.tsx:49`）、`text-accent-ink`（`ReadingText.tsx:58`）
- [ ] `hover:bg-surface` などの独自色＋状態の指定（59か所）が効いていない疑いが強い（Tailwind v4 は `@layer utilities` の自作クラスに `hover:` 等を付けない）。実機で確認し、トークンを `@theme inline` に登録して直す（§5）
- [ ] 暗い配色時代の紺色モーダル: `TradeModal.tsx:140`、旧ネオン色: `StockChart.tsx:57`、`MarketOverview.tsx:35`
- [ ] キーボード操作の枠を消している: `app/trade/page.tsx:241・348`
- [ ] 免責の共通部品化（§6-11）と `app/page.tsx:195` の文言修正

### P1：ルールの統一

- [ ] `app/globals.css` のトークンを §5 に差し替え（暖色オフホワイト→青みのオフホワイト、オレンジ→紺青、警告色の新設）
- [ ] トークンを通さない既製色の直書き（36ファイル・321か所）をトークンへ
- [ ] 「買い＝緑・売り＝赤」の残り: `app/page.tsx:38-43`、`app/watch/client.tsx:992-996`、`MasterSignals.tsx:26`、`InvestorPanel.tsx:18`、`app/trade/page.tsx:331・374`、`TradeButton.tsx:20・26`
- [ ] ブランド色が4系統（オレンジ・emerald・blue-600・ロゴ）→ 紺青1つへ: `page.tsx:85`、`trade/page.tsx:191`、`SiteNav.tsx:102`、`learn/page.tsx:772`、`review/page.tsx:308`
- [ ] 共通部品を作る: `Button`・`Card`・`Badge`・`Money`・`VirtualFundsTag`・`Disclaimer`（`components/ui/`）
- [ ] `components/chartTheme.ts` を唯一の色定義にし、`EquityChart.tsx:46` の線と `client.tsx:932` の凡例の色を一致させる
- [ ] 「くぼみ」の `--surface` をカード本体に使っている所を `--card` へ: `page.tsx:120`、`trade/page.tsx:209`
- [ ] 文字サイズ 12段階→8段階、角丸 5種→3種
- [ ] 太字 700 → 600、使われていない等幅書体（Geist Mono・`font-mono`）の除去
- [ ] スマホのアドレスバーの色が旧色のまま: `app/layout.tsx:55`
- [ ] `components/chartTheme.ts:73` が `--accent-ink` を読んでいる（トークン名を変えると黙ってオレンジに戻る）。トークン差し替えと同時に直す

### P1.5：囲いの撤去（A＋C へ）

- [ ] 数字タイル8枚の格子 → 表1つ: `app/watch/client.tsx:937,948`
- [ ] 数字タイル3枚 → 1行の数字: `app/review/page.tsx:272-291`
- [ ] 4段階を同形カード4枚で → 番号付きの一列: `app/page.tsx:115,120`
- [ ] 名人5人の同形カードの格子: `components/MasterSignals.tsx:97,101`
- [ ] 3段の入れ子（判断カード → 折りたたみ箱 → タイル4枚）: `components/watch/DecisionCard.tsx:77` → `components/analyze/DetailsSection.tsx:26` → `components/watch/FundamentalsFigure.tsx:123`
- [ ] 3段の入れ子（設定箱 → モード箱 → 選択肢3列）: `app/learn/page.tsx:639,729,739,835,864`
- [ ] 結果箱の中の中央揃えタイル5列: `components/analyze/MetricStrip.tsx:82,109`
- [ ] 左の小箱5つ: `app/watch/client.tsx:602-779`
- [ ] 空・読み込み中の中央揃えの大きな箱: `app/review/page.tsx:106,128,174,302,385`、`app/learn/page.tsx:982,1052`
- [ ] 字間を広げた英字風の段階ラベル: `app/trade/page.tsx:191`、`app/review/page.tsx:146`、`components/analyze/AnalyzeBanner.tsx:17`
- [ ] /trade の3つの節の箱（`209/275/318`）→ 見出しと区切り線。ピル型の札の多用: `/watch` 15か所、`DecisionCard.tsx:134,149`

### P2：画面の骨組み

- [ ] `/watch` の貼り付く帯の二重（`SiteNav.tsx:30`／`client.tsx:175`）、見出しの二重（`watch/page.tsx:17`／`client.tsx:180`）、スマホで見出しが端に接する（`watch/layout.tsx:8`）
- [ ] ナビの「● 運用中」の常時表示をやめる: `SiteNav.tsx:59`
- [ ] トップページを §3 の構成に（03 やる を心臓として描く）
- [ ] /watch に「分析の過程の再生」（§6-19）を組み込む。先に記録の追加（S2 と同じスライス）
- [ ] `/watch` の記録が無いときの画面にも免責を: `app/watch/client.tsx:431-459`

---

## 11. 未決事項（オーナーまたは担当部署の判断待ち）

1. **損益の ✓/✗ 記号**: 2026-09-10 の決定では損益に ✓/✗ を添えるが、「利益＝正しい判断」と読まれる恐れがある（原則11と逆向き）。符号（+/−）だけにするか → strategist・designer で再検討。
2. **`/watch` の成績の計器盤（勝率・シャープレシオ等）**: 残すか、畳むか、消すか。RULES.md #14 と近い → legal-compliance。
3. **トップの見出し**「リスクゼロ」（`page.tsx:90`）「うまくなるのは…あなたです」（`page.tsx:94`）の差し替え文言 → legal-compliance。
4. **文字サイズの切り替え機能**（PayPay カードの「普通/大/特大」[14]）を作るか。
5. **PayPay スタイルガイド本文の未確認点**: 公式ガイド [1] は取得ツールで本文を読めなかった。ボタン寸法や色の数値など、細部はオーナーがブラウザで開いて確認できる。本書の数値は PayPay の値の写しではなく、InvestSim 用に決めたもの。
6. `KNOWLEDGE.md:141` の古い主ペルソナ記述、`README.md` が雛形のまま。
7. **「実データ」と実装のずれ（見出しに「実データ」を掲げる前に解消が必要）**: 株価の取得に失敗すると架空の値に切り替わる箇所が残っている（`lib/ai-trader/engine.ts` 224・252-254・797・889・906 行、`app/stocks/[symbol]/page.tsx:23`、`app/api/signals/[symbol]/route.ts:15-17`、`lib/simulation.ts:100・110`。2026-09-11 legal-compliance 指摘・MC 未検証）→ builder。原則9違反の疑い。
8. **データ提供元の利用規約（弁護士確認・有料化前に必須）**: Yahoo は商用利用・無許可の自動取得を禁止、Twelve Data はサイト表示に再配布契約と出典表記が必要、J-Quants は個人の私的利用のみ（`lib/market/providers/jquants.ts` は使わない）。出典表記だけでは解消しない。
9. **AI の「▲ 買い」札を画面に出すこと自体が投資助言に当たるか**（弁護士確認）。
10. **目的の一文の表現**（弁護士確認）: 「〜できるようになる」が景品表示法の優良誤認に当たるか、無料のサービスにも同法がかかるか。legal-compliance の代案は「実データと仮想資金で、自分の投資判断を言葉にして残し、あとから答え合わせする練習場。」。オーナー判断で現状は原文。
11. ~~**チャートの系列色**（§5-1）が `validate_palette.js` 未通過の仮~~ → 2026-09-11 検査合格の3色に差し替え済み。
12. **/trade の段分け**（§6-9）: 今は1画面・確認の段なし。作り直すスライスで決め、DECISIONS に記録する → architect。
13. **「データが古い」の札を出すまでの時間**（§6-16） → data-engineer。

---

## 12. 出典（PayPay の公開情報。2026-09-11 researcher 調査）

ブランドの模倣（赤・ロゴの再現）ではなく、**考え方と部品の設計原則**だけを参考にしている。

1. PayPay App Style Guide — https://app-style-guide.paypay.ne.jp/app-styleguide
2. Goodpatch Blog（デザインシステム事例） — https://goodpatch.com/blog/2024-07-10cooldesignsystems
6. note（PayPay アプリの UI 観察） — https://note.com/shino_webdesign/n/n91120ba3d85f
7. PayPay Blog Designer Talk — https://blog.paypay.ne.jp/en/designer-talk/
8. ケータイ Watch（支払い方法の表示） — https://k-tai.watch.impress.co.jp/docs/news/2098895.html
10. PayPay Inside-Out Design at PayPay vol.10 — https://insideout.paypay.ne.jp/2025/11/11/design-at-paypay-vol10-jp/
11. PayPay UXライター求人 — https://www.green-japan.com/company/9605/job/260933
14. PayPayカード 文字サイズ変更 — https://www.paypay-card.co.jp/info/004105.html
15. PayPay 支払い画面の変更 — https://paypay.ne.jp/notice/20240408/f-paymentscreen/
19. PayPay ポイント運用 — https://paypay.ne.jp/article/point-management/

（番号は調査メモの通し番号のまま。本書で参照していない番号は省略）
