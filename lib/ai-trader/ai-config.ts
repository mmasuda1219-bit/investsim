// AI 呼び出しの設定値（純データ）。engine.ts と画面（/watch の「分析の過程の再生」）の両方がここを読む。
//
// engine.ts は child_process と server-only を読み込むためクライアント部品から実体 import できない。
// 画面が「今の設定は 40 秒で打ち切り・返事の上限 3,500 トークン」と書くとき、engine.ts の定数を
// 手で書き写すと将来ずれるので、値の置き場所をここに一本化する（engine.ts 側もここを参照する）。
// I/O・環境変数・副作用を持ち込まないこと（クライアント・サーバー・tsx のどこからでも安全に読める）。

/**
 * Claude に判断を求めるときの打ち切り時間（ms）。根拠は engine.ts の callClaudeApi の注釈。
 *
 * S4-1(2026-09-15) 35秒 → 40秒。cron の 50秒枠の内訳は
 *   前段（候補の走査・データ取得・知識の読み込み）約2秒 ＋ AI 最大40秒 ＋ 失敗時の記録保存 5秒 ＝ 47秒。
 * 残り3秒は関数の起動・後処理の余白。ここを 40秒より伸ばすと枠を割るので、
 * それ以上必要になったら「ストリーミングで途中まで救出する」方に進む（上限を足さない）。
 */
export const CLAUDE_TIMEOUT_MS = 40_000

/**
 * 判断1回の返事の上限トークン数。根拠は engine.ts の注釈（絞りすぎると JSON が途中で切れる）。
 *
 * S4-1(2026-09-15) 2500 → 3500。本番の過程の記録（9/14 の3回）が3回とも max_tokens で、
 * 1判断 約310〜385トークン × 9銘柄 ＝ 約2,800〜3,500 必要だった実測による。
 * 併せてプロンプト側で字数の上限と「数値の再掲禁止」を課し、1判断を約230トークンに縮める
 * （engine.ts の【返し方】を参照）。出力は約100〜115トークン/秒なので 3,500 でも約30秒で収まる。
 */
export const DECISION_MAX_TOKENS = 3500
