# AI開発カンパニー OS（lean版）

> 目的: アプリ開発を「効率的かつ賢く」進めるための最小の会社設計。
> 設計思想: **人数を増やすことは効率化ではない。** 役職（サブエージェント）はコンテキストを汚さないための仕組みであり、トークンコストは増える（サブエージェント中心の運用で約7倍という報告あり）。よって「複雑さが閾値を超えた所だけ」役職を使う。
> 想定ランタイム: Claude Code / 想定プロダクト: Instagram DM 自動化 SaaS 本体

---

## 0. 作り始める前に決めること（ここが未確定なら下は作らない）

この会社は「作るアプリ」が決まって初めて意味を持つ。組織を先に作るのは投機。先に下を埋める。

- [ ] **このアプリの最初の「縦切り1本」は何か？**（1文で。例: 「ManyChatのDM受信→GPT意図分類→3分岐のうち1分岐だけ自動返信、が end-to-end で動く」）
  - → 1文で書けないなら、会社を作るのは時期尚早。先にこれを決める。
- [ ] **ランタイムは Claude Code でよいか？**（違うなら下の「Claude Codeへの落とし込み」は読み替え）
- [ ] **人間のゲートは誰が・いつ押すか？**（あなた / 弟。計画承認と出荷前の2点）
- [ ] **これは SaaS 本体か、別アプリか？**（別なら原則の例を差し替え）

---

## 1. 原則（Constitution）— 最重要・全役職が従う

役職より原則が効く。これは CLAUDE.md にそのまま入れる「憲法」。

1. **目的優先**: 全タスクは「今のゴール」に紐づく。紐づかないならやらない。作業の冒頭に必ずゴールを1文で宣言する。
2. **最小の縦切り**: 横の足場（スキャフォールド）を広げる前に、end-to-end で1本通す。通る道がない横展開は禁止。
3. **決定を先に**: 設計・方針は「決めて・記録してから」コードを書く。作ってから再決定はしない（手戻り最大の原因）。
4. **同じ過ちを繰り返さない**: 非自明な修正・決定はすべて `DECISIONS.md` に1行残す。再実装の前に必ず参照する。
5. **客観と網羅、ただし時間制限つき**: 決定の前に「代替案＋トレードオフ」を並べる。が、網羅は決定に奉仕するためであり、完全性のためではない。タイムボックスを切る。
6. **レビューは別の目で**: 実装者は自分のコードを自分で承認しない。reviewer 役またはクリーンな文脈で見る。
7. **コンテキストを汚さない**: ノイズの多い探索は隔離ワーカーに委譲する。親セッションは「ゴール＋決定ログ＋今のdiff」だけを保持する。
8. **動くものが正義**: 磨くのは出荷するものだけ。早すぎる抽象化・早すぎる汎用化は禁止。

---

## 2. 役職（Roles）— 最小セット。各役職は「思考モード」

> ⚠️ 増やしたくなったら一度止まる。営業役・CFO役などの追加は演劇化の入口。**営業は弟（人間）であってエージェントではない。**
> 最初に作る価値があるのは **architect と reviewer の2つだけ**。残りは詰まってから足す。

| 役職 | 役割（思考モード） | 書込権限 | 出力（Definition of Done） |
|---|---|---|---|
| **orchestrator** | あなた＋親セッション。ゴール保持・分解・決定。自分ではコードを書かず委譲する | — | 分解されたタスク列／承認判断 |
| **architect** | 機能を渡されたら「方針＋ファイル計画＋リスク＋ADR-lite」を出す。コードは書かない | 読み取りのみ | 方針1段落＋変更ファイル一覧＋リスク3点＋`DECISIONS.md`追記案 |
| **builder** | 定義された1スライスのコード＋テストを書く。計画にあるファイルだけに限定 | 書込＋実行 | 動くコード＋テスト緑＋変更要約 |
| **reviewer** | バグ・セキュリティ・スコープ逸脱を探す | 読み取りのみ | 重大度別の指摘リスト（critical/warning/suggestion） |
| **researcher**（任意・並列・安価モデル） | 外部API/ドキュメント（Meta/Instagram Graph API, ManyChat, Make）を調べる | 読み取りのみ | 要点ダイジェスト（出典付き） |

各役職プロンプトの鉄則: **(a) 何を担当し何を担当しないかを明示** / **(b) 出力フォーマットを指定** / **(c) スコープを境界づける**（「渡された変更ファイルだけ見る」等）。

---

## 3. スキル（Skills）— 再利用する知識/手順パック

スキルは「オンデマンドで読み込まれる再利用プロンプト」。役職に `skills:` で紐づける。本プロジェクト候補:

- `ig-dm-api-conventions` … Meta/Instagram メッセージングAPIの規約・レート制限・webhookパターン
- `error-handling-patterns` … 自分の標準のエラー/リトライ規約
- `decision-log` … ADR-lite（1エントリの書き方）
- `review-checklist` … セキュリティ/バグ/スコープのチェック項目

対応: architect→`decision-log` / reviewer→`review-checklist` / builder→`error-handling-patterns` `ig-dm-api-conventions` / researcher→`ig-dm-api-conventions`

---

## 4. 運用ループ（The Loop）

```
ゴール宣言
  → researcher（外部の未知があれば／並列・安価モデル）
  → architect（方針＋ADR）
  → 【人間ゲート①: あなたが計画を承認】
  → builder（コード＋テスト）
  → reviewer（指摘）
  → 【人間ゲート②: 出荷前承認】
  → スライス出荷 → DECISIONS.md に記録 → 次のスライスへ
```

- **人間ゲートは2点だけ**（計画承認・出荷前）。ここを省くとエージェントが暴走する。
- ループは「縦切り1本」単位で回す。1本通ってから次の1本。

---

## 5. Claude Code への落とし込み（2026年6月時点の仕様。`/agents` で最新確認推奨）

| 設計要素 | 置き場所 |
|---|---|
| 原則（§1） | `CLAUDE.md`（プロジェクトルート、毎セッション読まれる） |
| 各役職（§2） | `.claude/agents/<name>.md`（プロジェクト）or `~/.claude/agents/<name>.md`（個人） |
| スキル（§3） | `SKILL.md`（YAML frontmatter＋本文）。役職に `skills:` で注入 |

サブエージェントは **Markdown＋YAML frontmatter**。必須は `name` と `description`。`description` は「ラベル」ではなく**トリアージ規則**として書く（「〜のときに使う。〜を返す」）と自動委譲が安定する。`tools` はセキュリティのレバー（省略すると全ツール継承）、`model` はコストのレバー（探索はHaiku、実装はSonnet/Opus）。

### frontmatter 例

```markdown
---
name: architect
description: 機能の実装方針を立てるときに使う。コードは書かず、方針・変更ファイル計画・リスク・ADR-lite案を返す。
tools: Read, Grep, Glob
model: sonnet
skills:
  - decision-log
---
あなたはアーキテクトです。コードは書きません。
渡された機能について、以下だけを出力します:
1. 方針（1段落）
2. 変更するファイル一覧
3. リスク上位3点
4. DECISIONS.md への追記案（1〜2行）
スコープ外の機能には触れないこと。
```

```markdown
---
name: reviewer
description: 直近のdiffをレビューするときに使う。バグ・セキュリティ・スコープ逸脱を重大度別に返す。コードは書かない。
tools: Read, Grep, Glob, Bash
model: sonnet
skills:
  - review-checklist
---
あなたはコードレビュアーです。書き込みはしません。
git diff を見て、変更ファイルだけを対象に、
critical / warning / suggestion の3段階で
ファイル:行 と修正案を添えて返してください。
```

builder は書込が要るので `tools: Read, Edit, Write, Bash`、researcher は `tools: Read, WebFetch` ＋ `model: haiku` で安く。

---

## 6. コスト/罠メモ（自分への注意書き）

- **役職を使うのは「文脈を汚す・出力が大きい・並列で得する」作業だけ。** 単純作業に役職を噛ませるとトークンだけ増えて遅くなる。
- **最小スタート**: いきなり全役職を作らない。まず `CLAUDE.md`（原則）＋ループを手動で回す。コンテキスト肥大が実際に効き始めたら architect / reviewer を足す。
- frontmatter の細部（`skills:` 等）はバージョンで変わりうる。`/agents` の生成UIで確認すること。

---

## 確証の弱い点（明示）

- §5 の frontmatter 仕様は2026年6月時点の公開ドキュメント/記事に基づく。`skills:`・`model:` フィールドは複数ソースで確認できたが、Claude Code は更新が速いので**手元の `/agents` で最終確認**すること。
- 「サブエージェント約7倍トークン」は記事ベースの目安であり、あなたの実ワークロードでは変動する。
